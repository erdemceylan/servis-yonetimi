<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Product Template Form View - Dövizli Satış Fiyatı Alanlarını Ekle -->
    <record id="product_template_form_view_inherit_multi_currency" model="ir.ui.view">
        <field name="name">product.template.form.inherit.multi.currency</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_only_form_view"/>
        <field name="arch" type="xml">
            <!-- Satış Fiyatından sonra dövizli satış fiyat alanlarını ekle -->
            <xpath expr="//field[@name='list_price']" position="after">
                <div class="row mt-3 mb-3">
                    <div class="col-lg-6">
                        <label for="custom_list_price" string="Dövizli Satış Fiyatı"/>
                        <div class="o_row">
                            <field name="custom_list_price" class="oe_inline" widget="float" placeholder="Döviz cinsinden fiyat" style="flex: 1; margin-right: 10px;"/>
                            <field name="custom_currency_id" class="oe_inline" options="{'no_create': True, 'no_open': True}" placeholder="Para Birimi Seçin" style="flex: 0 0 150px;"/>
                        </div>
                    </div>
                    <div class="col-lg-6">
                        <p class="text-muted" style="font-size: 12px; margin-top: 20px;">
                            💡                            <strong>Otomatik Dönüşüm:</strong> Döviz cinsinden fiyat girin. 
                            Odoo otomatik olarak bugünün kurunu kullanarak TL cinsinden Satış Fiyatını hesaplayacaktır.
                        </p>
                    </div>
                </div>
            </xpath>

            <!-- Maliyet Fiyatından sonra dövizli maliyet alanlarını ekle -->
            <xpath expr="//field[@name='standard_price']" position="after">
                <div class="row mt-3 mb-3">
                    <div class="col-lg-6">
                        <label for="custom_cost_price" string="Dövizli Maliyet Fiyatı"/>
                        <div class="o_row">
                            <field name="custom_cost_price" class="oe_inline" widget="float" placeholder="Döviz cinsinden maliyet" style="flex: 1; margin-right: 10px;"/>
                            <field name="custom_cost_currency_id" class="oe_inline" options="{'no_create': True, 'no_open': True}" placeholder="Para Birimi Seçin" style="flex: 0 0 150px;"/>
                        </div>
                    </div>
                    <div class="col-lg-6">
                        <p class="text-muted" style="font-size: 12px; margin-top: 20px;">
                            💡                            <strong>Otomatik Dönüşüm:</strong> Döviz cinsinden maliyet girin. 
                            Odoo otomatik olarak bugünün kurunu kullanarak TL cinsinden Maliyet Fiyatını hesaplayacaktır.
                        </p>
                    </div>
                </div>
            </xpath>
        </field>
    </record>

    <!-- Product Template List View - Dövizli Fiyat Kolonlarını Ekle -->
    <record id="product_template_tree_view_inherit_multi_currency" model="ir.ui.view">
        <field name="name">product.template.tree.inherit.multi.currency</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_tree_view"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='list_price']" position="after">
                <field name="custom_list_price_display" optional="hide" class="o_field_small"/>
            </xpath>
            <xpath expr="//field[@name='standard_price']" position="after">
                <field name="custom_cost_price_display" optional="hide" class="o_field_small"/>
            </xpath>
        </field>
    </record>
</odoo>




